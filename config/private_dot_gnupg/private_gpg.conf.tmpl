# Key Retrieval Settings

# Automatically retrieves missing public keys from a keyserver when verifying signatures.
auto-key-retrieve

# Output Settings

# Prevents GPG from including the version number in the ASCII-armored output.
# This improves privacy by not disclosing the software version.
no-emit-version

# Default Key Settings

# Sets the default GPG key (by its fingerprint or ID) for signing and encryption.
default-key {{ .gpgkey }}
